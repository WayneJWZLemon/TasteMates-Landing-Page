import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  base: "/", // if serving without custom domain with GitHub page then "/TasteMates-Landing-Page/"
});
